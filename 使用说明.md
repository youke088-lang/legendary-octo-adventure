# YUKI豪宅代理工作站 — 安装说明

这个文件夹是一个完整的 PWA（渐进式网页应用），装好之后就是手机桌面上一个**真正的 App 图标**：
- 点开没有浏览器地址栏，全屏运行
- 断网也能正常打开、录入、保存
- 不依赖任何随时会失效的下载链接

## 重要提醒：为什么需要"部署"这一步

手机浏览器（无论 iPhone 还是安卓）出于安全限制，**不允许**直接从本地文件里开启"离线缓存"功能（Service Worker 必须通过网址 https 访问才能生效）。所以要用满血版体验，需要把这个文件夹放到一个网址上——但这**不等于**依赖一个随时失效的链接，因为下面推荐的是你自己**永久拥有、免费、不会过期**的地址，和 Claude 对话里的临时下载链接完全是两回事。

## 部署方法（5分钟，免费，永久有效）

**推荐：Netlify Drop（最简单，不需要注册也能用）**

1. 电脑浏览器打开：https://app.netlify.com/drop
2. 把这个文件夹（连同里面所有文件）直接拖进网页
3. 几秒钟后会生成一个网址，例如 `xxxx.netlify.app`
4. 手机浏览器打开这个网址

之后想要地址永久固定、方便管理，可以免费注册 Netlify 账号认领这个站点，网址就永久归你了，谁都不能收回。

**手机上安装成 App 图标**

- iPhone（Safari 打开网址）：点击底部分享按钮 → "添加到主屏幕"
- 安卓（Chrome 打开网址）：右上角菜单 → "安装应用" 或 "添加到主屏幕"

安装完成后，桌面会出现"YUKI代理"图标，点开即是全屏 App，此后**关闭网络也能正常使用**（首次打开时会自动把所有文件缓存到手机本地）。

## 数据保存说明

- 所有房源、客户、带看记录数据保存在这台设备的浏览器本地存储里，永久保留，不会因为断网、关机、重装 App（只要不清除浏览器数据）而丢失
- App 内"设置"里有"导出/导入数据"功能，建议定期导出 JSON 备份，换设备时可以直接导入，数据无缝迁移
- 数据只存在本机，不会自动同步到其他设备；如果需要多设备同步，需要额外的后端服务，这是纯本地版本

## 文件说明

- `index.html` — App 主体（界面与全部功能逻辑）
- `manifest.json` — App 安装配置（图标、名称、启动方式）
- `service-worker.js` — 离线缓存脚本，实现断网可用
- `icon-192.png` / `icon-512.png` / `icon-512-maskable.png` / `apple-touch-icon.png` — App 图标
